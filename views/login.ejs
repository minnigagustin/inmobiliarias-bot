<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Acceso | BR-Group</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
  <style>
    @import url('https://api.fontshare.com/v2/css?f[]=clash-display@700,600,500&f[]=general-sans@400,500,600&display=swap');

    :root {
      --bg-primary: #0a0a0f;
      --bg-secondary: #12121a;
      --bg-elevated: #1a1a24;
      --accent-gold: #d4a853;
      --accent-gold-dim: rgba(212, 168, 83, 0.15);
      --accent-gold-glow: rgba(212, 168, 83, 0.4);
      --text-primary: #fafafa;
      --text-secondary: #71717a;
      --text-muted: #3f3f46;
      --border: rgba(255, 255, 255, 0.06);
      --error: #f87171;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body { height: 100%; font-family: 'General Sans', sans-serif; background: var(--bg-primary); color: var(--text-primary); overflow: hidden; }

    .container { display: grid; grid-template-columns: 1fr 1fr; height: 100vh; }

    /* Panel Visual */
    .visual-panel {
      position: relative;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 3rem;
      background: linear-gradient(145deg, #0f0f18 0%, #0a0a0f 100%);
      overflow: hidden;
    }

    .pattern-bg {
      position: absolute;
      inset: 0;
      opacity: 0.03;
      background-image: linear-gradient(var(--accent-gold) 1px, transparent 1px), linear-gradient(90deg, var(--accent-gold) 1px, transparent 1px);
      background-size: 60px 60px;
      mask-image: radial-gradient(ellipse at center, black 20%, transparent 70%);
    }

    .hero-shape {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 400px;
      height: 400px;
    }

    .hero-shape::before, .hero-shape::after {
      content: '';
      position: absolute;
      border: 1px solid var(--accent-gold);
      border-radius: 50%;
      animation: pulse-ring 4s ease-in-out infinite;
    }

    .hero-shape::before { inset: 0; opacity: 0.2; }
    .hero-shape::after { inset: 40px; opacity: 0.15; animation-delay: 0.5s; }

    @keyframes pulse-ring {
      0%, 100% { transform: scale(1); opacity: 0.2; }
      50% { transform: scale(1.05); opacity: 0.1; }
    }

    .logo-mark {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .logo-mark svg {
      width: 80px;
      height: 80px;
      color: var(--accent-gold);
      filter: drop-shadow(0 0 40px var(--accent-gold-glow));
      animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    .visual-header { position: relative; z-index: 10; }
    .visual-header .eyebrow {
      font-family: 'Space Mono', monospace;
      font-size: 0.7rem;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--accent-gold);
      margin-bottom: 0.75rem;
    }

    .visual-header h2 {
      font-family: 'Clash Display', sans-serif;
      font-size: 2.5rem;
      font-weight: 600;
      line-height: 1.1;
    }

    .visual-footer { position: relative; z-index: 10; }
    .visual-footer p { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.6; max-width: 280px; }

    .stats { display: flex; gap: 2rem; margin-top: 1.5rem; }
    .stat-value { font-family: 'Clash Display', sans-serif; font-size: 1.75rem; font-weight: 600; }
    .stat-label { font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-muted); margin-top: 0.25rem; }

    /* Panel Form */
    .form-panel {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem;
      background: var(--bg-secondary);
      position: relative;
    }

    .form-panel::before {
      content: '';
      position: absolute;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      opacity: 0.03;
      pointer-events: none;
    }

    .form-container { width: 100%; max-width: 380px; position: relative; z-index: 10; animation: fadeSlideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1); }

    @keyframes fadeSlideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .form-brand { margin-bottom: 3rem; }
    .form-brand h1 { font-family: 'Clash Display', sans-serif; font-size: 2rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 0.5rem; }
    .form-brand h1 span { color: var(--accent-gold); }
    .form-brand p { font-size: 0.95rem; color: var(--text-secondary); }

    form { display: flex; flex-direction: column; gap: 1.5rem; }

    .input-group label {
      display: block;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      color: var(--text-secondary);
      margin-bottom: 0.6rem;
    }

    .input-wrapper { position: relative; }

    .input-wrapper input {
      width: 100%;
      padding: 1rem 1rem 1rem 3rem;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      border-radius: 12px;
      color: var(--text-primary);
      font-size: 0.95rem;
      font-family: inherit;
      transition: all 0.25s;
      outline: none;
    }

    .input-wrapper input::placeholder { color: var(--text-muted); }
    .input-wrapper input:focus { border-color: var(--accent-gold); background: rgba(212, 168, 83, 0.03); box-shadow: 0 0 0 4px var(--accent-gold-dim); }

    .input-icon {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      width: 18px;
      height: 18px;
      color: var(--text-muted);
      transition: color 0.25s;
      pointer-events: none;
    }

    .input-wrapper input:focus ~ .input-icon { color: var(--accent-gold); }

    .btn-submit {
      margin-top: 0.5rem;
      padding: 1rem 1.5rem;
      background: var(--accent-gold);
      border: none;
      border-radius: 12px;
      color: var(--bg-primary);
      font-family: 'Clash Display', sans-serif;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-submit:hover { transform: translateY(-2px); box-shadow: 0 8px 30px var(--accent-gold-glow); }

    .alert-error {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem;
      background: rgba(248, 113, 113, 0.1);
      border: 1px solid rgba(248, 113, 113, 0.2);
      border-radius: 10px;
      color: var(--error);
      font-size: 0.875rem;
      margin-bottom: 1rem;
      animation: shake 0.4s;
    }

    .alert-error svg { flex-shrink: 0; width: 18px; height: 18px; }

    @keyframes shake {
      0%, 100% { transform: translateX(0); }
      20% { transform: translateX(-8px); }
      40% { transform: translateX(8px); }
      60% { transform: translateX(-4px); }
      80% { transform: translateX(4px); }
    }

    .form-footer { margin-top: 3rem; padding-top: 1.5rem; border-top: 1px solid var(--border); text-align: center; }
    .form-footer p { font-size: 0.75rem; color: var(--text-muted); }

    @media (max-width: 900px) {
      .container { grid-template-columns: 1fr; }
      .visual-panel { display: none; }
      .form-panel { padding: 2rem; }
      .form-brand { text-align: center; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="visual-panel">
      <div class="pattern-bg"></div>
      <div class="visual-header">
        <p class="eyebrow">Panel de Control</p>
        <h2>Sistema de<br>Atención<br>Inteligente</h2>
      </div>
      <div class="hero-shape">
        <div class="logo-mark">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
          </svg>
        </div>
      </div>
      <div class="visual-footer">
        <p>Plataforma omnicanal para gestión de conversaciones y atención automatizada con IA.</p>
        <div class="stats">
          <div class="stat-item"><span class="stat-value">24/7</span><span class="stat-label">Disponible</span></div>
          <div class="stat-item"><span class="stat-value">IA</span><span class="stat-label">Asistida</span></div>
        </div>
      </div>
    </div>

    <div class="form-panel">
      <div class="form-container">
        <div class="form-brand">
          <h1>BR<span>.</span>Group</h1>
          <p>Ingresá tus credenciales para continuar</p>
        </div>

        <% if (error) { %>
          <div class="alert-error">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
            <span><%= error %></span>
          </div>
        <% } %>

        <form action="/login" method="POST">
          <div class="input-group">
            <label for="username">Usuario</label>
            <div class="input-wrapper">
              <input type="text" id="username" name="username" placeholder="Ingresá tu usuario" required autocomplete="username" autofocus>
              <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            </div>
          </div>

          <div class="input-group">
            <label for="password">Contraseña</label>
            <div class="input-wrapper">
              <input type="password" id="password" name="password" placeholder="Ingresá tu contraseña" required autocomplete="current-password">
              <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
            </div>
          </div>

          <button type="submit" class="btn-submit">Iniciar Sesión</button>
        </form>

        <div class="form-footer">
          <p>&copy; <%= new Date().getFullYear() %> BR-Group. Todos los derechos reservados.</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
