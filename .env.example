# ===========================================
# BR-Group WhatsApp Bot - Variables de Entorno
# ===========================================

# --- OBLIGATORIO ---
# Secreto para sesiones (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your_super_secret_key_here_at_least_32_chars

# --- SERVIDOR ---
PORT=3000
NODE_ENV=development  # 'development' o 'production'

# --- OpenAI (para NLU/Chat IA) ---
OPENAI_API_KEY=sk-your-openai-api-key-here

# --- WhatsApp Bridge ---
# URL del servidor web (server.js) para el bridge de WhatsApp
BRIDGE_URL=http://localhost:3000/bridge

# --- Notificaciones WhatsApp (opcional) ---
# Número de agente para recibir notificaciones directas (sin +, ej: 5491112345678)
AGENT_NUMBER=

# --- WordPress API (Propiedades) ---
WP_BASE=https://lginmobiliaria.com.ar
WP_TIMEOUT_MS=9000
WP_CACHE_TTL_MS=600000

# --- Branding ---
COMPANY_NAME=BR-Group
BOT_NAME=asistente virtual

# --- Seguridad (Fase 2) ---
# Orígenes CORS permitidos (separados por coma)
CORS_ORIGINS=http://localhost:3000,https://backpackpuntaalta.ar
